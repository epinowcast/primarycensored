% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitdistdoublecens.R
\name{.fitdist_discretehazard}
\alias{.fitdist_discretehazard}
\title{Build and fit the discretehazard (logit-hazard random walk) non-parametric
model}
\usage{
.fitdist_discretehazard(
  delays,
  params,
  K,
  boundaries,
  dprimary,
  dprimary_args,
  N,
  dots
)
}
\description{
Free parameters: alpha, log_sigma, eps_1, ..., eps_\{K-1\}.
logit_h_i = alpha + sigma * cumsum(c(0, eps))[i], sigma = exp(log_sigma).
h_K is forced to 1. Prior penalty (amortised over N observations):
eps_j ~ N(0, 1), alpha ~ N(0, 5), log_sigma ~ N(log(0.5), 1).
Each density value is multiplied by exp(-prior_per_obs) so fitdistrplus
minimises -loglik + prior_penalty as a single objective.
}
\keyword{internal}
