% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcens.R
\name{update.pcens}
\alias{update.pcens}
\title{Update the parameters of a pcens object}
\usage{
\method{update}{pcens}(object, ..., primary_args = NULL)
}
\arguments{
\item{object}{A \code{pcens} object as created by \code{\link[=new_pcens]{new_pcens()}}.}

\item{...}{Named delay distribution parameters. Each one replaces the
entry of the same name in \code{object$args}, or is added if not present.
Parameters that are not given keep their current values.}

\item{primary_args}{Optional named list of primary event distribution
arguments. These are merged into \code{object$primary_args} in the same way
as \code{...} is merged into \code{object$args}. Defaults to \code{NULL}, which leaves
the primary event distribution arguments unchanged.}
}
\value{
A \code{pcens} object with the same class as \code{object} and updated
\code{args}, \code{primary_args} and \code{dprimary_args} fields. See \code{\link[=new_pcens]{new_pcens()}} for
the fields of a \code{pcens} object.
}
\description{
Replaces the delay distribution parameters, and optionally the primary
event distribution arguments, of an existing \code{pcens} object.
The delay and primary distribution functions, the primary event CDF and
the class are kept as they are.
The updated object therefore dispatches to the same \code{\link[=pcens_cdf]{pcens_cdf()}} method
without looking up names or rebuilding the class.
This is cheaper than calling \code{\link[=new_pcens]{new_pcens()}} again when one distribution is
evaluated for many parameter sets, for example posterior draws.
}
\details{
Parameters are merged rather than replaced as a whole, so
\code{update(object, scale = 3)} changes \code{scale} and keeps all other
parameters. Parameters cannot be removed; use \code{\link[=new_pcens]{new_pcens()}} for that.

A name in \code{...} that is not already in \code{object$args} must be an argument
of \code{object$pdist} other than its first, unless \code{pdist} takes \code{...}.
Otherwise an error is raised. Names in \code{primary_args} are not checked
against \code{dprimary}.
}
\examples{
obj <- new_pcens(
  pdist = pgamma, dprimary = dunif,
  primary_args = list(min = 0, max = 1),
  shape = 1, scale = 1
)
obj <- update(obj, shape = 2, scale = 3)
pcens_cdf(obj, q = c(1, 5, 10), pwindow = 1)

# Update the primary event distribution arguments
obj <- new_pcens(
  pdist = pgamma, dprimary = dexpgrowth,
  primary_args = list(r = 0.2),
  shape = 2, scale = 3
)
obj <- update(obj, primary_args = list(r = 0.5))
pcens_pmf(obj, x = 0:5, pwindow = 1)
}
\seealso{
Low level primary event censored distribution objects and methods
\code{\link{new_pcens}()},
\code{\link{pcens_cdf}()},
\code{\link{pcens_cdf.default}()},
\code{\link{pcens_cdf.pcens_pdiscretehazard}()},
\code{\link{pcens_cdf.pcens_pdiscretestep}()},
\code{\link{pcens_cdf.pcens_pgamma_dunif}()},
\code{\link{pcens_cdf.pcens_pgengamma.orig_dunif}()},
\code{\link{pcens_cdf.pcens_pgengamma_dunif}()},
\code{\link{pcens_cdf.pcens_plnorm_dunif}()},
\code{\link{pcens_cdf.pcens_pweibull_dunif}()},
\code{\link{pcens_pmf}()},
\code{\link{pcens_pmf.default}()},
\code{\link{pcens_quantile}()},
\code{\link{pcens_quantile.default}()}
}
\concept{pcens}
