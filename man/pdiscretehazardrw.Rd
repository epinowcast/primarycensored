% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdiscretehazard.R
\name{pdiscretehazardrw}
\alias{pdiscretehazardrw}
\alias{ddiscretehazardrw}
\alias{rdiscretehazardrw}
\alias{pdiscretehazardre}
\alias{ddiscretehazardre}
\alias{rdiscretehazardre}
\title{Hazard-parameterised step CDF aliases (RW and RE variants)}
\usage{
pdiscretehazardrw(q, boundaries = NULL, hazards)

ddiscretehazardrw(x, boundaries = NULL, hazards)

rdiscretehazardrw(n, boundaries = NULL, hazards)

pdiscretehazardre(q, boundaries = NULL, hazards)

ddiscretehazardre(x, boundaries = NULL, hazards)

rdiscretehazardre(n, boundaries = NULL, hazards)
}
\arguments{
\item{q}{Numeric vector of quantiles.}

\item{boundaries}{Numeric vector of length \eqn{K+1} defining the bin
edges. Must be strictly increasing. Defaults to \code{0:K}.}

\item{hazards}{Numeric vector of length \eqn{K} (or \eqn{K-1}, in which
case a trailing 1 is appended) giving the discrete-time conditional
hazard for each bin. Values must be in \eqn{[0, 1]}.}

\item{x}{Numeric vector of values at which to evaluate the PMF.}

\item{n}{Integer. Number of samples to draw.}
}
\value{
Numeric vector of CDF (or PMF, or sample) values; see the
underlying function.
}
\description{
Thin aliases of \code{\link[=pdiscretehazard]{pdiscretehazard()}}/\code{\link[=ddiscretehazard]{ddiscretehazard()}}/\code{\link[=rdiscretehazard]{rdiscretehazard()}}.
Outside fitting they are byte-identical: the dist family is fully
specified by the hazard vector. They differ at the fitting layer in
\code{\link[=fitdistdoublecens]{fitdistdoublecens()}}:
}
\details{
\itemize{
\item \code{pdiscretehazardrw} uses the random-walk transform
\code{logit(h_i) = alpha + sigma * cumsum(eps)} (default for
\code{distr = "discretehazard"}).
\item \code{pdiscretehazardre} uses an IID logit random-effect transform
\code{logit(h_i) = alpha + sigma * eps_i}.
}
}
\examples{
hazards <- c(0.3, 0.5, 1)
pdiscretehazardre(c(0.5, 1, 2, 3), boundaries = 0:3, hazards = hazards)
}
\seealso{
Other pdiscretehazard:
\code{\link{ddiscretehazard}()},
\code{\link{pdiscretehazard}()},
\code{\link{rdiscretehazard}()}
}
\concept{pdiscretehazard}
