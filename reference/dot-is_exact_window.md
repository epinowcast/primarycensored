# Test for a zero-width censoring window

Test for a zero-width censoring window

## Usage

``` r
.is_exact_window(window)
```

## Arguments

- window:

  A censoring window width.

## Value

`TRUE` if `window` is a single value equal to zero.
