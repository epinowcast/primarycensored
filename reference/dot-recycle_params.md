# Recycle observation settings to a given length

Recycle observation settings to a given length

## Usage

``` r
.recycle_params(params, n)
```

## Arguments

- params:

  A data frame of per-observation settings.

- n:

  Number of rows to return.

## Value

`params` with its rows recycled or cut to `n` rows.
