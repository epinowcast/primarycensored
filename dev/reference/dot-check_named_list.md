# Check that every element of a list is named

Check that every element of a list is named

## Usage

``` r
.check_named_list(x, what)
```

## Arguments

- x:

  A list.

- what:

  Character string describing `x` for the error message.

## Value

`NULL` invisibly. Called for its error.
